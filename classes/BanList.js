class BanList {
  constructor({
    borders: ['silver'],
    sets: [],
    alaCard: [],
    rules: ['Ante'],
  }) {

    this.borders = borders;
    this.sets = sets;
    this.alaCard = alaCard;
    this.rules = rules;
  }

  checkCard(card) {
    
  }
}